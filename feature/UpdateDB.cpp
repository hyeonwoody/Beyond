#include "../Beyond.h"

bool CJob::CSubJob::updateDB (unsigned int type, bool isFlag){
    switch(type){
        case 0:
            if (isFlag){

            }
            else{

            }
        case 1:
            if (isFlag){

            }
            else {
                
            }
    }
    return true;
}